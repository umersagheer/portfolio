# X post copy

Attach the WebSockets Remotion promo video to this post.

## Ready to post

Most "WebSocket tutorials" stop at `socket.on('message')`. They never show you the binary frame format, why ghost connections eat your memory, or how to scale past a single server.

I wrote a deep dive covering the full stack: HTTP upgrade handshake, opcodes & ping-pong heartbeats, the Envelope Pattern (~120 bytes vs ~800 bytes HTTP overhead), backpressure, and Redis pub/sub for horizontal scaling — with 7 interactive demos you can break:
https://umersagheer.dev/posts/websockets
