# X post copy

Attach the Layout Animations Remotion promo video to this post.

## Ready to post

Why do `width`/`height` animations feel janky while `transform` feels smooth?

I broke down the browser pipeline, FLIP, `layout`, `layoutId`, and `AnimatePresence` in one post, with interactive demos:
https://umersagheer.dev/posts/layout-animations
