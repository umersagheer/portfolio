---
  title: 'Git, Under the Hood: An Interactive Tour of How Git Really Works'
  summary: 'Stop memorizing Git commands and start understanding them. A hands-on walk through the object store and the pointers on top of it — blobs, trees, commits, branches, merge, rebase, reset, and reflog — with interactive demos you can poke at.'
  image: /images/posts/git-internals-cover.png
  author: 'Umer Sagheer'
  publishedAt: '2026-07-17'
  category: 'post'
---

{/*
  ============================================================================
  BUILD SCAFFOLD — Git Internals blog.
  Phase 0 = this outline: every heading + a 1–2 sentence intent note + commented
  component placeholders. Real prose and the interactive components land phase by
  phase (see repo-root plan.md). Only Phase 1's component (HashPlayground) is
  wired now; the rest stay commented until their phase.
  ============================================================================
*/}

You use Git every day, and most days it just works — until the day it doesn't.
A rebase goes sideways at 11pm, a branch seems to vanish, and suddenly you're
copy-pasting commands from Stack Overflow and hoping.

The fix isn't memorizing more commands. It's a mental model. Underneath every
command, Git is doing something surprisingly simple and consistent. Once you can
picture the little **objects** it stores and the **pointers** it moves around,
the scary commands stop being magic — they become obvious.

This post builds that picture from the ground up, and it's meant to be *played
with*. Every big idea comes with something you can poke at and watch respond.

## Part 1 — The Object Store

{/* Intent: frame Part 1. Git is, at heart, a tiny key-value store of content.
    Everything else is built on top. This is a short bridge into the storage model. */}

### What lives inside .git

{/* Intent: the four things that make a repo — objects/, refs/, HEAD, index.
    Delete this one folder and the history is gone; it IS the database. Plain-language
    tour, no component (yet — a small annotated file listing in prose). */}

### Content-addressable storage

Here's the one idea that everything else in Git is built on: **Git names things by
their content.**

Normally you name a file first (`report.txt`) and then decide what goes in it. Git
does the opposite. You hand it some content, and it hands you back a **hash** — a
short-looking fingerprint of exactly those bytes. That hash becomes the _address_
Git files the content under. Same bytes, same address. This is called
**content-addressable storage**, and it's why the same word keeps showing up
throughout this post.

Play with it below. Type into either file and watch its hash recompute the instant
the content changes:

<HashPlayground />

Two things fall out of this, and you can see both in the demo:

- **Identical content is stored only once.** If two files (or two hundred, across a
  hundred commits) contain the same bytes, they hash to the same address, so Git
  keeps a single copy. Make the two files above match and watch them collapse into
  one shared object. This is why a repo with thousands of commits isn't thousands of
  full copies of your project.
- **Corruption can't hide.** Because the address is derived from the content, if a
  single byte of a stored object ever changed, its hash would no longer match the
  name it's filed under — and Git would notice immediately. Try flipping one
  character with the preset buttons: the whole hash scrambles. That sensitivity is a
  feature.

So Git isn't really a pile of files with names. It's a **key-value store** where the
key is a hash of the value. Everything from here on — every commit, branch, and
merge — is just objects in this store and pointers between them.

### The three kinds of objects

We said Git is a key-value store of content. But content alone isn't enough — you also
need filenames, folders, and a sense of history. Git builds all of that out of just
**three kinds of objects**, and they stack neatly on top of each other.

**A blob is raw content.** When you stage a file, Git stores its bytes as a _blob_ — and
that's it. A blob has no name, no path, no timestamp. It's a naked chunk of content,
addressed by its hash. Two files with identical contents share one blob (that's the
dedup you saw above).

**A tree gives blobs their names.** If blobs have no filenames, where do names live? In a
_tree_ object. A tree is basically a directory listing: a set of entries, each mapping a
name to a hash. `app.py → <blob hash>`, `utils.py → <blob hash>`. A tree can also point at
_other_ trees — that's how Git represents subfolders. So a tree is a snapshot of one
directory, and trees-pointing-at-trees is your whole project structure.

**A commit wraps a tree in history.** A _commit_ ties everything together. It points at a
single tree (the complete snapshot of your project at that moment) and adds the metadata
you actually think of as "a commit": the author, the message, and — crucially — the hash
of the **parent** commit that came before it. That parent pointer is what strings commits
into a history.

Here's the thing to internalize: `git add` and `git commit` do different jobs.
**`git add` writes a blob** (right then, the moment you stage). **`git commit` is what
builds the tree and the commit object** and moves your branch forward. The workbench below
lets you watch exactly that happen. Edit a file, stage it, give it a message, and commit —
and keep your eye on the object store on the right:

<ObjectInspector />

Drive the editor at the bottom and keep your eye on the `.git` panel up top — it has two
sides: your **files** on the left, and the **object store** (what Git actually keeps) on
the right. Try this:

- **Edit `app.py`** in the editor. It shows up as a _modified file_ on the left — still just
  a file. The hash Git _would_ give its contents shows next to the filename in the editor.
- **Open the Source Control tab** (the branch icon) and **check the file to stage it.** Watch
  a blue **blob** appear in the object store on the right — that's all `git add` really is:
  it turns your file into a stored object.
- **Write a message and hit Commit.** _Now_ a purple **tree** and a new **commit** appear,
  the commit links back to its parent, and `main`/`HEAD` slide forward together.
- **Change the message** and commit again: the commit hash comes out different, because the
  message is part of what gets hashed.
- **`git reset --hard`** moves the branch pointer back one commit — and notice the commit you
  just made doesn't vanish. It becomes _unreferenced_ (we'll rescue exactly this kind of
  "lost" commit with the reflog near the end of the post).

### Snapshots, not diffs

Most people picture a commit as a _diff_ — a little bundle of "what changed since last time."
That's how the tools _show_ history, so it's a reasonable guess. But it's not what Git stores.

**Every commit is a complete snapshot of your whole project.** Its tree names every file, and
every file points at a blob — not just the ones you touched. That sounds hopelessly wasteful, but
remember the very first idea in this post: Git names blobs by their content. So a file you _didn't_
change hashes to the _same blob_ it did last commit, and Git already has that object. The new
snapshot just points at it again. **Unchanged content is shared, never copied** — which is why a
repo with a thousand commits isn't a thousand copies of your project.

So where do diffs come from? Git computes them **on the fly**, by comparing two snapshots, right
when you run `git diff` or `git log -p`. The diff is a _view_, generated on demand and thrown away
— never written to disk. Step through this little history and flip between the two: what Git
**stores** (snapshots, with unchanged files reusing the same blob) versus what Git **shows** you
(a freshly computed diff).

<SnapshotVsDiff />

Notice that on each step, at least one file carries its blob hash over unchanged — that's the
dedup doing its job. And the diff panel is honest about it: for an unchanged file there's simply
nothing to compute, because both snapshots already point at the same object.

## Part 2 — Pointers &amp; Commands

The object store from Part 1 is the hard part, and you've got it: blobs, trees, commits, all named
by their content and sharing whatever they can. Here's the good news — **everything else Git does
is just two moves**: _write a new object_ and _move a pointer_. Merge, rebase, reset, reflog — none
of them are new magic. They're combinations of those two moves over the graph of commits. So let's
look at that graph first.

### The commit graph (DAG)

Every commit stores the hash of the commit that came before it — its **parent**. That single
backward pointer is the thread that turns a pile of commits into a history. Follow the parents from
any commit and you walk backwards through time, one commit at a time, until you reach the very first
commit (which has no parent).

Because commits only ever point _backwards_, the shape you get can branch and rejoin but can never
loop — you can't be your own ancestor. Mathematicians call that a **directed acyclic graph**, or
DAG; think of it as a family tree where each commit knows its parent. Two rules are worth holding
onto: **arrows point child → parent** (backwards in time, because a commit knows what came before
it, not after), and a **merge** commit is the only kind with **two** parents — it's where two lines
of history tie back together.

Build one yourself. **Commit** extends the current line, **branch** forks a new line off the current
commit, and **merge** joins another branch back in. Watch the arrows: they always reach back toward
older commits, and the merge commit is the one with two of them.

<DagExplorer />

Every button here only ever _adds_ objects — a commit, or a new branch name. Nothing already in the
graph is edited; that immutability is the whole reason Git can hand you the same commit hash forever
and the reflog can dig up "lost" work later on.

### Branches are just pointers

{/* Intent: a branch is a 41-byte file holding one commit hash. HEAD is a pointer to a
    branch (or, detached, straight to a commit). Committing = write object, move branch,
    HEAD follows. Show the literal .git/HEAD and refs/heads/main file contents. */}

{/* <BranchHeadSimulator /> — Phase 4 */}

### The staging area (index)

{/* Intent: the waiting room between working directory and history. working dir → git add
    (blob written) → index → git commit (trees + commit written). Likely folded into the
    flagship ObjectInspector; called out here so the narrative order is intact. */}

### Merge

{/* Intent: fast-forward (no divergence → just slide the pointer, zero new objects) vs
    3-way merge (both sides moved → one brand-new merge commit with two parents). */}

{/* <MergeSimulator /> — Phase 4 */}

### Rebase

{/* Intent: "moving commits" is a lie — objects are immutable. Rebase REPLAYS commits as
    brand-new objects with new hashes and new parents; the originals are orphaned. This is
    why you never rebase shared history. */}

{/* <RebaseReplay /> — Phase 5 */}

### Reset and revert

{/* Intent: reset moves a branch pointer; soft/mixed/hard differ only in what they do to the
    index and working directory. Revert moves nothing — it adds a new inverse commit on top. */}

{/* <ResetModes /> — Phase 5 */}

### Reflog — the safety net

{/* Intent: Git journals every move HEAD makes (~90 days). "Lost" commits are only
    unreferenced, still sitting in the object store. Recover one by finding its hash and
    pointing a branch at it. Nothing is ever truly deleted. */}

{/* <ReflogRescue /> — Phase 5 */}

## Wrapping up

{/* Intent: one-paragraph recap — it's all an object store (blobs/trees/commits) plus
    pointers (branches/HEAD) moving across a DAG. Every command is some combination of
    "write an object" and "move a pointer." */}

### Further Reading

{/* Intent: curate in a later phase. Seed list below. */}

- [How Git Works Under the Hood](https://www.youtube.com/watch?v=Ala6PHlYjmw) — video walkthrough of the object model
- [Git Internals — Commits, Branches, Merge, Rebase, Reflog](https://www.youtube.com/watch?v=Csd4lMKPC5g) — video on the pointer mechanics
- [Pro Git — Git Internals: Git Objects](https://git-scm.com/book/en/v2/Git-Internals-Git-Objects) — the canonical reference
- [A Visual Guide to Git Internals](https://www.freecodecamp.org/news/git-internals-objects-branches-create-repo/) — objects, branches, and building a repo by hand
