import { brandDomain } from '../../shared/brand'

export const timezonesCoverContent = {
  title: 'Where Did 5 Orders Go?',
  subtitle:
    'Two rules to fix timezone bugs.\nPast events -> UTC. Future events -> wall time + IANA timezone.',
  footerDomain: brandDomain,
} as const
