import { brandDomain } from '../../shared/brand'

export const gitInternalsCoverContent = {
  title: 'Git,\nUnder the Hood',
  subtitle: 'The object store and the pointers on top —\nblobs, trees, commits, branches, HEAD.',
  footerDomain: brandDomain,
} as const
