import { brandDomain } from '../../shared/brand'

export const gitResetModesCoverContent = {
  title: 'git reset,\nDemystified',
  subtitle: 'One command, one dial — --soft, --mixed, --hard.\nAnd why only one of them can lose your work.',
  footerDomain: brandDomain,
} as const
